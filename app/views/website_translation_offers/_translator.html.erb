<%= translator_profile(
        translator,
        status_controls(translator, @translator_contracts[translator], @minimum_bid_amount, @maximum_bid_amount),
        infos_controls(translator, @translator_contracts[translator])
    ) %>