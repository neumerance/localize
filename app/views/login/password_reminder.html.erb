<p><%= _('Your old password cannot be retrieved, but we can reset it and send you a new one.') %></p>
<p><%= _("Enter your email and we'll send you a password reset link.") %></p>
<br />
<%= form_tag({:action=>'send_password'}) do %>
	<p>E-Mail:
	<input type="text" maxlength="45" size="45" name="email" />
	</p>
	<input type="submit" value="Submit" />
<% end %>
<div class="inter_tab_spacer"></div>
<p><%= link_to(_('Back to home page'),'/') %> &nbsp; | &nbsp; <%= link_to(_('Login'), {:action=>:index}) %></p>
