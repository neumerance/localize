<div class="content">
<%= my_error_messages_for :bookmark %>

<%= form_for(:bookmark, :url => bookmarks_path) do |f| %>
	<p>You can leave a note which will help you recognize this user later.</p>
	<p>This note will also be visible on <%= link_to(_("%s's page")%@auser.full_name,{:controller=>:users, :action=>:show, :id=>@auser.id, :anchor=>:bookmarks}) %> and serves as public feedback.</p>
    <p>
	<b>Note</b><br />
    <%= f.text_area :note, :rows => 6, :cols => 70, maxlength: COMMON_NOTE %>
    <%= f.hidden_field :resource_id %>
  </p>

  <p>
    <%= submit_tag 'Create bookmark', data: { disable_with: 'Create bookmark' } %>
  </p>
<% end %>

<%= link_to 'Back to My Bookmarks', bookmarks_path %>
</div>
