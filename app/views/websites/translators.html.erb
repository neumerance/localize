<% if @translators.length > 0 %>
	<p>Found <%= @translators.length %> <%= @source_lang.name %> to <%= @target_lang.name %> translators in ICanLocalize.</p>
	<p>Click on each translator for full profile. You can also invite translators to apply for your projects from their profile pages.</p>
	
	<%= render(:partial=>'translator', :collection=>@translators) %>
<% else %>
	<p>There are currently no <%= @source_lang.name %> to <%= @target_lang.name %> translators in ICanLocalize.
	If you have a project for this language pair, please <%= link_to('contact us', {:controller=>:support, :action=>'new'}) %>.</p>
<% end %>