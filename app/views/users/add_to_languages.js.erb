<% if @warning %>
  alert('<%= @warning %>')
<% elsif @reload %>
  location.reload()
<% else %>
  jQuery("#to_languages").html('<%= escape_javascript(render :partial => "to_languages") %>')
  <% if @dest_id %>
    jQuery('html, body').animate({
      scrollTop: jQuery("#<%= @dest_id %>").offset().top
    }, 2000);
  <% end %>
<% end %>