<p>Dear <%= @user.full_real_name %>,</p>

<p>The issue '<%= @issue.title %>' involving you has changed status. It's now: <%= Issue::STATUS_TEXT[@issue.status] %></p>

<p>
  <% link = url_for({:escape => false, :only_path => false, :host=>EMAIL_LINK_HOST, :protocol=>EMAIL_LINK_PROTOCOL, :controller=>:issues, :action=>:show, :id=>@issue.id } ) %>
  To view the full details of this issue, click on this link: <%= link_to link, link %>
</p>

<%= EMAIL_SIGNATURE %>
