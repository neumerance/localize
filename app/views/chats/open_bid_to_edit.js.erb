<% if @update_bid %>
	jQuery('<% "#lang#{@lang_id}" %>').html('<%= escape_javascript(render :partial => 'show_bid', :object => @bid_disp) %>')
  setTimeout(function(){
    location.reload()
  }, 2000)
<% end %>